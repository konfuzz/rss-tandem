# Дата: 2026-03-28

- Реализация нового лидерборда на фронтенде.
- Адаптация UI лидерборда под мобильные экраны и новый ответ сервера.
- Отдельная работа с банком вопросов и перераспределением категорий/типов.

## Что сделано по лидерборду

- На ветке `feat/implement-leaderboard` реализована новая страница лидерборда поверх текущего backend endpoint `/leaders`.
  - сервер сам присылает готовый top-10
  - пагинацию решили не реализовывать
  - фронтенд только показывает данные и помечает текущего пользователя
  - табы переключения между метриками
  - мотивационное сообщение, если текущий пользователь не попал в top-10
- Добавлены три метрики:
  - `maxStreak`
  - `finished`
  - `avgScore`
- Добавлена интеграция в приложение:
  - новый route `/leaderboard`
  - ссылка на лидерборд в навигации


## Работа с банком вопросов

- Приведены категории вопросов к согласованному списку от Андрея. Все темы, которых не было в новом списке категорий, были переразложены по допустимым категориям.
- Собрана сводка по количеству вопросов:
  - по категориям
  - по типам
  - по связке "категория x тип"
- Посчитан дефицит по покрытию типов в категориях.
- На основе этого была расширена база вопросов:
  - новые `id` были продолжены от `245`
  - итоговый объём банка составил `389` вопросов
  - Категории приведены к единой структуре.
  - Покрытие типов по категориям стало ровнее